export enum EPlatform {
  JOBDA = 'JOBDA',
  JOBDA_CMS = 'JOBDA_CMS',
}

export enum EPlatformToNum {
  JOBDA = 0,
  JOBDA_CMS = 1,
}

export enum EPlatformToColor {
  JOBDA = '#D3E5EF',
  JOBDA_CMS = '#F5E0E9',
}
