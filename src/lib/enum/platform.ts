export enum Platform {
  JOBDA = 'JOBDA',
  JOBDA_CMS = 'JOBDA_CMS',
}

export enum PlatformToNum {
  JOBDA = 0,
  JOBDA_CMS = 1,
}

export enum PlatformToColor {
  JOBDA = '#D3E5EF',
  JOBDA_CMS = '#F5E0E9',
}
